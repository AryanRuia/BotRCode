# Frontend (Vite + React)

Install and run locally:

1. cd frontend
2. npm ci
3. npm run dev

Build for production:

npm run build

The build output will be in `dist/` and the top-level `setup.sh` copies that to `backend/static/` for serving from the backend.
